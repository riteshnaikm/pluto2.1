# Prompt templates (experiments)

JD and resume-evaluation prompt drafts used during development. **Not loaded automatically at runtime** unless wired in `app.py` — treat as reference material for prompt engineering.

Files were moved from `JD_Modified_prompt/` at repo reorganisation.
